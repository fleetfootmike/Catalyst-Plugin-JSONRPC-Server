# Catalyst::Plugin::JSONRPC::Server

Generic JSON-RPC 2.0 server plugin for Catalyst. App-agnostic: it knows the
JSON-RPC protocol, nothing about your domain.

See the POD in `lib/Catalyst/Plugin/JSONRPC/Server.pm` for usage.
